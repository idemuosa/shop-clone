rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // DEBUG MODE: Allow read/write for now to fix your permission errors immediately
    // We will restrict these later once the dashboard is working.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
